{"ts":"2026-05-15T05:00:31Z","mode":"ux-polish","outcome":"shipped","title":"Framerate-overrides: warn-pill badge + honest Settings copy when override diverges from global","rationale":"User-trust tier: breaks-trust (Settings copy was actively false — 'every running instance is throttled at this rate uniformly' the moment any account had an override) + erodes-trust (override badge existed but was invisible: mono-micro white-0.85 inside a cyan gradient launch button). Surface: SettingsView Frame rate section + AccountsListView per-row badge in splitLaunchButton. Five rough patches found in the focused scan; bundled top two (copy fix + warn-pill render) as one polish since they form the same trap's two surfaces. Cart-detection: light flow — own muscle for the 4-task plan; superpowers brainstorming + writing-plans + subagent-driven-development per task with two-stage review (spec compliance + code quality) gates; finishing-a-development-branch for PR. Worktree-isolated execution. NOTE: this branch is off origin/main and contains a fresh atlas.jsonl with only this entry; the FFlag PR (#2) carries the FFlag competitive entry separately. Merge order is independent — both will need a small concat-resolve when both land on main.","rejected_runners_up":["Settings: surface 'N accounts override this cap' (queued, trust=3, would catch the override at moment of setting global)","Per-account chevron menu rename to 'Frame rate cap override (this account)' (queued, trust=1, names the relationship)","Launch-time transient signal when effective cap ≠ global (queued, trust=3, heaviest — new UI surface)"],"pr":"https://github.com/estevanhernandez-stack-ed/rororo-mac/pull/3"}
